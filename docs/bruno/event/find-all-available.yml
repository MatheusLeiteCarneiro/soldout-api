info:
  name: find-all-available
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
