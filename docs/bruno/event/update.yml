info:
  name: update
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{host}}/events/{{eventId}}"
  body:
    type: json
    data: |-
      {
        "name": "Updated event name",
        "description": "Updated Event description",
        "startsAt": "2027-08-28T16:00:00Z",
        "endsAt": "2027-08-30T16:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
