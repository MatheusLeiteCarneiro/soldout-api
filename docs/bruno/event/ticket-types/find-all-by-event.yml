info:
  name: find-all-by-event
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/events/{{eventId}}/ticket-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
