info:
  name: publish
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/events/{{eventId}}/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
