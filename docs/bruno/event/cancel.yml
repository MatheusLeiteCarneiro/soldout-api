info:
  name: cancel
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/events/{{eventId}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
