info:
  name: publish
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/events/publish/{{eventId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
