info:
  name: cancel
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/events/{{eventId}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
