info:
  name: find-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/events/{{eventId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
