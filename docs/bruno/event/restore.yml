info:
  name: restore
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/events/{{eventId}}/restore"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
