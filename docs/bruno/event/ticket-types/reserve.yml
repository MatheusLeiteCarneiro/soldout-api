info:
  name: reserve
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/ticket-types/{{ticketTypeId}}/reserve"
  body:
    type: json
    data: |-
      {
        "quantity": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
